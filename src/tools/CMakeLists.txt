INCLUDE_DIRECTORIES(${CUDA_INCLUDE_DIRS}  )
CUDA_ADD_EXECUTABLE(simplecublas simpleCUBLAS.c)
CUDA_ADD_CUBLAS_TO_TARGET(simplecublas)

ADD_EXECUTABLE(cudaTest cudaTest.cpp)
TARGET_LINK_LIBRARIES(cudaTest ECuda)
